# Exam Proctoring System

This app monitors online exams for tab switch, copy-paste, and warning tracking.

## Tech Stack
- React + Tailwind frontend
- Express + PostgreSQL backend

## Setup
1. Set up `.env` with your database URL
2. Run backend: `cd server && npm install && npm start`
3. Run frontend: `cd client && npm install && npm start`
